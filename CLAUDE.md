# Game Changer — contexte de projet

Application de coaching pour un club de football amateur/jeunes, développée avec Claude sur claude.ai depuis plus de 13 sessions, puis avec Claude Code directement dans le dépôt (depuis sept. 2026). Ce fichier consolide ce qui a été appris — à lire avant toute recommandation structurelle, ajout ou modification de fonctionnalité.

Gregory (le porteur du projet, coach au club) travaille en français, en tutoiement.

## Le projet

- React + Vite, déployé sur Vercel (projet `gcfa/game-changer`, redéployé automatiquement à chaque push sur `main`) : `football-analysis-ten.vercel.app`
- Dépôt GitHub : `GregCohin/game-changer` — **dépôt public** : jamais de secret, de clé privée, d'email ni de donnée de joueur ou de famille dans le dépôt
- Couvre l'ensemble des opérations du club : administratif, vestiaire (effectif/séances/médical), analyse vidéo, ressources pédagogiques, portail joueur/parent
- Backend Supabase (depuis sept. 2026), uniquement pour le portail parent et le forum : voir la section « Backend Supabase » plus bas
- Pipeline d'analyse vidéo (Python/Colab : calibration terrain PnLCalib sur images Veo Cam 3, suivi et ré-identification des joueurs, OCR des numéros de maillot) : **dans ce dépôt depuis le 12/09/2026**, dossier `video-pipeline/` (`extract.py`, `tracking.py`, `calibration.py`, `metrics.py`, notebook Colab `colab_extraction.ipynb` pour le GPU). Son historique se lit dans `git log -- video-pipeline` ; les derniers commits touchent aussi `App.jsx` (import des données de tracking)

## Architecture — à connaître avant toute recommandation structurelle

**Le site staff n'a ni serveur, ni base de données, ni authentification** — à l'exception du portail parent et du forum, qui ont depuis sept. 2026 un vrai backend Supabase (section dédiée plus bas). Vérifié directement dans le code : sur ~35 000 lignes, les seuls appels réseau de l'app staff sont une requête météo (open-meteo), le chargement à la demande du moteur ffmpeg depuis un CDN, et les appels Supabase de `src/lib/portalSync.js`. Tout le reste — chaque fonctionnalité, y compris celles qui semblent collaboratives — lit et écrit uniquement dans `localStorage` du navigateur (ou dans IndexedDB pour les matchs et les clips).

Implications concrètes, pas théoriques :
- Côté staff, le forum, le covoiturage, les créneaux bénévoles et la FAQ ne fonctionnent réellement que si une seule personne, sur un seul appareil, gère toutes les données : `localStorage` ne se synchronise jamais entre navigateurs ou appareils différents. Pour les parents, le portail (données de l'enfant, carnet de bord, covoiturage, FAQ) et le forum passent désormais par Supabase, alimenté par une publication manuelle du staff — les créneaux bénévoles n'en font pas partie.
- Si le club a plusieurs coachs sur des appareils séparés, chacun n'a que les données de sa propre équipe dans son navigateur — les vues "transversales à toutes les équipes" (Vue académie, Tableau de bord dirigeant, Ressources partagées, Talents à suivre) ne sont complètes que pour la personne dont c'est le navigateur. Le backend Supabase ne change pas ça : il ne synchronise que staff → parents, et deux personnes qui publieraient la même équipe/saison depuis des navigateurs différents s'écraseraient mutuellement.
- `lib/storage.js` patch `localStorage` globalement pour scoper chaque clé par équipe + saison, sauf une liste explicite de clés non-scopées (club-wide, dans `UNSCOPED_STORAGE_KEYS`). Une saison ne s'écrase jamais — toute proposition supposant une perte de données entre saisons part d'une fausse prémisse. Depuis sept. 2026 le patch ne s'applique qu'à `window.localStorage` (garde ajoutée : `Storage.prototype` est aussi celui de `sessionStorage`) et signale par un `alert()`, une seule fois par session, un dépassement de quota (`QuotaExceededError`) qui était auparavant avalé en silence.
- Un embryon de couche async-ready existe déjà pour un futur vrai backend (`getAllReferees`/`saveReferee`, prototype de mise en relation d'arbitres entre clubs) : écrit en asynchrone dès maintenant bien qu'il ne fasse que lire/écrire du local pour l'instant, précisément pour que le jour où une API existe, seul l'intérieur de ces fonctions change. Ce principe a servi pour le backend Supabase : `src/lib/portalSync.js`.

**Direction produit souhaitée** : le site reste l'outil de travail complet du staff/coachs/dirigeants. Les parents/joueurs auraient une interface séparée, plus simple, filtrée sur ce qui les concerne (leur enfant, leur équipe/catégorie). Objectif à terme : une app mobile **native** (préférée à une PWA). Les deux — interface simplifiée ET app native — demandent un vrai backend avec authentification comme prérequis commun. Recommandation suivie (sept. 2026) : ce backend a été construit pour une seule fonctionnalité ciblée d'abord — le portail parent, avec le forum — plutôt que de migrer tout le site d'un coup. L'interface parent web actuelle est volontairement minimale ; l'app native reste l'objectif à terme et réutilisera la même base et la même authentification. **Vision multi-clubs (précisée par Gregory le 19/09/2026)** : à terme, plusieurs clubs utiliseront le produit. Conséquences : le domaine d'envoi des emails et l'adresse du portail sont ceux du produit, pas ceux d'un club (voir « Email et connexion ») ; et le modèle actuel est mono-club — aucune notion de club dans le schéma Supabase, `staff_profiles` est global (`is_staff()` ouvre toutes les données) et `localStorage` ne contient qu'un club par navigateur. Ouvrir le produit à d'autres clubs demandera d'introduire le club (droits par club dans la RLS) : à garder en tête avant toute recommandation structurelle.

## Backend Supabase (portail parent + forum)

Ajouté en sept. 2026, uniquement pour ces deux fonctionnalités : le reste du site reste en `localStorage`. Vérifié de bout en bout en production avec l'effectif réel. La connexion d'un parent réel (adresse autre que celle de Gregory) est maintenant techniquement possible — SMTP personnalisé branché, authentification jugée parfaite par tous les outils — mais pas encore fiable partout : l'email atterrit toujours en indésirables sur iCloud. Voir « Email et connexion ».

**Ce qui existe**
- Projet Supabase `game-changer` (réf. `dajsfquzknvutugjkrye`, région Paris, offre gratuite : contrainte de budget de Gregory), URL `https://dajsfquzknvutugjkrye.supabase.co`. Les variables `VITE_SUPABASE_URL` et `VITE_SUPABASE_ANON_KEY` (publiques par conception : c'est la RLS qui protège) sont dans `.env.local` en local et dans Vercel (Production, Preview, Development).
- **Aucun secret dans le dépôt** : la clé `service_role`, le jeton d'accès Supabase et le mot de passe de la base n'y sont pas — les redemander à Gregory ou les régénérer depuis le dashboard Supabase.
- Schéma, RLS et fonction `redeem_invitation_code` : `supabase/migrations/`, appliqués avec `npx supabase db push` (jeton d'accès et mot de passe de la base requis). Une migration déjà appliquée ne se réécrit jamais : on en ajoute une.
- Côté staff : écran `PortalBackendScreen` (Administratif → Club → « Portail parent (backend) », dans `App.jsx` à côté de `BackupScreen`) et `buildPortalSnapshot`. Boutons « Publier vers le portail » et « Récupérer les nouveautés », génération de codes d'invitation, liste des parents liés.
- Côté parent : `parent.html` → `src/parent/`, second point d'entrée Vite (`vite.config.js`). Connexion par lien magique, saisie du code, portail (statut, objectifs, séances, matchs, carnet de bord, covoiturage, FAQ) et forum. Style volontairement minimal. **Confort téléphone** : `parent.html` porte des règles CSS globales — champs à 16 px (en dessous, Safari sur iPhone zoome la page au toucher d'un champ), boutons/champs/sélecteurs à 44 px de haut minimum, bouton désactivé grisé, coupure des mots sans espace (`overflow-wrap`, sinon un lien collé dans une note élargit toute la page) — que tout nouvel écran parent hérite ; un `fontSize`/`minHeight` inline les écraserait. `RTP_STAGE_LABELS` (`PortailScreen.jsx`) recopie `RTP_STAGES` d'`App.jsx` (l'app parent n'importe jamais App.jsx) : ajouter une étape côté staff demande de l'ajouter aussi là, sinon le parent ne voit que « Blessure en cours » (jamais la clé technique).

**Modèle d'accès**
- Connexion par lien magique, sans mot de passe. Un compte staff est un compte présent dans `staff_profiles` (fonction SQL `is_staff()`) ; seul celui de Gregory y est aujourd'hui, et en ajouter un se fait par insertion manuelle (pas d'auto-inscription, volontairement).
- Un parent est lié à un enfant par un code d'invitation à 6 caractères généré par le staff (`player_invitation_codes`, 5 utilisations par défaut : plusieurs parents indépendants par enfant), consommé par `redeem_invitation_code` (SECURITY DEFINER). Aucune policy n'autorise un parent à insérer directement dans `parent_player_links` (seuls la fonction et le staff le peuvent).
- Un parent ne voit que ses enfants liés et les données de leur équipe/saison. Une conversation de forum « individuelle » n'est visible que par ses participants, **figés** (`forum_thread_participants`) à la première publication du thread : un parent lié plus tard ne voit jamais les anciennes conversations privées (voulu par Gregory).
- Le portail n'expose que le statut RTP des blessures — jamais diagnostic, dossier médical, discipline ni contacts d'urgence. À conserver dans `buildPortalSnapshot`, mais la vraie barrière, ce sont les policies RLS, pas le composant.
- Forum : un message de parent s'affiche sous « Parent de <prénom de l'enfant> », **jamais sous son email** (dans un sujet d'équipe, tous les parents lisent les messages des autres) ; les messages du staff portent une pastille « Staff » (`author_kind`, pas le nom, qui est libre). Le staff ne peut écrire, modifier ou supprimer que des messages `author_kind = 'staff'` : la base refuse d'écraser un message de parent.
- Covoiturage : un enfant ne s'inscrit qu'une fois par offre (contrainte d'unicité), la limite de places est appliquée par la base (trigger `enforce_carpool_capacity`, verrou sur l'offre : deux parents ne peuvent pas prendre la dernière place) et un parent ne peut inscrire que ses enfants liés, sur les offres de leur équipe. L'écran affiche « Complet » mais ce n'est plus lui qui protège.

**Synchronisation (manuelle, dans les deux sens)**
- Staff → parents, « Publier » : `development_goals`, `individual_programs`, `injuries`, `sessions`, `club_faq`, `competitions`/`fixtures` sont remplacées en bloc par (équipe, saison), `club_events` (club-wide) en entier. `players`, `matches`, `match_stats`, `carpool_offers` et `forum_threads` sont en **upsert seulement** : leurs enfants écrits par les parents (`player_journal_entries`, `carpool_passengers`, `forum_messages`) partent en cascade avec elles, un delete-then-insert les effacerait. `players` et `matches` doivent être publiés avant les tables qui les référencent. Les messages du staff (`tf_forum_messages`) sont aussi publiés, en upsert, avec `author_kind = 'staff'` ; `portalSync` écarte tout identifiant déjà connu comme message de parent, car `pullPortalUpdates` ramène les messages des parents dans ce même tableau local.
- Parents → staff, « Récupérer les nouveautés » : ramène journal, covoiturage et messages de forum dans le `localStorage` du staff (curseur `tf_portal_last_pull`).
- Limites connues : un joueur, un match, un sujet ou un message supprimé côté staff n'est pas supprimé de Supabase par la publication ; les pièces jointes des messages (exercices, clips) ne sont pas publiées ; « Récupérer » n'ajoute que — un passager qui se désinscrit reste dans la liste locale du staff ; les passagers de covoiturage saisis avant la migration (de simples noms) ne sont pas repris ; `club_events.location` n'est pas résolue ; publier depuis un navigateur qui n'a pas les mêmes événements club efface ceux de Supabase (`club_events` est remplacée en entier) ; le curseur de récupération avance même quand elle ne renvoie rien (une RLS cassée le masque — vider `tf_portal_last_pull` pour rejouer).

**Email et connexion**
- Le service d'email par défaut de Supabase est utilisé en repli. **Il ne délivre qu'aux adresses des membres de l'équipe du projet** et limite à 2 messages par heure (doc Supabase, « Send emails with custom SMTP »). Ne jamais ajouter des parents à l'équipe Supabase : ils auraient accès au tableau de bord. `site_url` est `https://football-analysis-ten.vercel.app` et la liste blanche de redirections contient la prod et `http://localhost:5173/**` — sans ça, les liens magiques renvoyaient vers `localhost:3000`.
- **Domaine du produit acquis** le 19/09/2026 : `game-changer.fr`, chez OVH, compte « Particulier » au nom de Gregory (à transférer si une structure porte le produit un jour), payé 3 ans d'un coup (renouvellement 19/09/2029, pas un abonnement annuel comme prévu au départ — pas un problème, juste à savoir). `gamechanger.com` (enregistré depuis 2002 par un tiers, messagerie d'entreprise dessus) et `gamechanger.eu` sont tous deux garés en vente sur des places de marché de noms de domaine (GoDaddy/Sedo) — n'appartiennent à personne d'utile pour ce projet, jamais à utiliser comme expéditeur.
- **Brevo, configuré en secours** : authentifié sur `game-changer.fr` (2 CNAME DKIM `brevo1._domainkey`/`brevo2._domainkey`, 1 TXT `brevo-code`, 1 TXT `_dmarc`, chez OVH), expéditeur `noreply@game-changer.fr`, modèles « Confirm signup » et « Magic Link » réécrits en français directement dans Supabase (Authentication → Emails → Templates). **Piège rencontré** : coller un texte HTML long dans le presse-papiers via une commande shell (`pbcopy`) peut corrompre l'encodage des accents (`à` devient un caractère illisible) sans que rien ne le signale — vérifié en relisant le presse-papiers avec `osascript -e 'get the clipboard as text'`, pas seulement `pbpaste` ; correctif définitif : écrire les accents en entités HTML (`&agrave;`, `&eacute;`…) pour rester en ASCII pur. Score mail-tester.com : 7,5/10 — pénalisé par le suivi que Brevo ajoute (pixels sans texte alternatif, lien du bouton réécrit vers un domaine Brevo) et l'en-tête `List-Unsubscribe` obligatoire ; ni l'un ni l'autre n'est désactivable sur l'offre actuelle (le suivi des emails transactionnels ne peut être qu'anonymisé, jamais coupé ; l'en-tête `List-Unsubscribe` n'est remplaçable que sur l'offre Enterprise).
- **Resend, SMTP actif en production aujourd'hui** — changé par Gregory directement dans Supabase, jamais lu directement dans ses réglages ici : déduit de la trace technique d'un email reçu (infrastructure `amazonses.com`, MX `rsend.game-changer.fr`). Domaine vérifié par seulement 3 enregistrements chez OVH (1 TXT `resend._domainkey`, 2 CNAME `rsend`/`send`) — Resend a simplifié sa méthode depuis la documentation généraliste, qui décrit encore un MX + un TXT SPF en plus, devenus inutiles. Réglages SMTP : host `smtp.resend.com`, port `465`, utilisateur `resend`, mot de passe = une clé API Resend (jamais partagée dans le chat). Score mail-tester.com : 10/10 — aucun suivi, aucun `List-Unsubscribe`, lien direct vers Supabase sans réécriture.
- **Ce qui reste ouvert : réputation, pas configuration.** iCloud classe encore l'email en indésirables malgré le 10/10 et une authentification (SPF/DKIM/DMARC) jugée parfaite par tous les outils. `game-changer.fr` n'avait que 2-3 jours d'existence et aucun historique d'envoi au moment du test ; iCloud est réputé particulièrement strict avec les domaines récents, indépendamment de la configuration technique. Pas de correctif technique connu — seulement du temps et un envoi régulier légitime (éviter aussi les rafales de tests rapprochés, qui peuvent ressembler à un envoi automatisé). Gmail pas encore testé. En attendant : dire aux premières vraies familles de vérifier leurs indésirables au premier email et de le marquer « Non indésirable ».

**Tester sans boîte mail** : `POST /auth/v1/admin/generate_link` avec la clé `service_role`, suivre `/auth/v1/verify` pour lire le `location` (`access_token`, `refresh_token`), puis écrire la session dans le `localStorage` du domaine testé sous `sb-<réf>-auth-token`. Toujours supprimer les comptes et lignes de test ensuite. Le classifieur de permissions de Claude Code bloque ces appels : demander l'accord de Gregory à chaque fois.

**Tester sans rien casser de réel** : publier depuis le vrai scope avec un jeu de test remplacerait les données réelles. Passer par un scope isolé (`tf_active_team` = `e2e-team` et `tf_active_season` = `e2e-season` dans le `localStorage` du navigateur de test, puis semer les clés `tf_*`), vérifier avant de publier que `club_events` (club-wide, remplacée en entier) est vide, et tout supprimer ensuite — par ordre : offres de covoiturage, sujets de forum, joueurs (la suppression se propage aux tables filles ; `carpool_passengers.player_id` n'a pas de cascade, d'où l'ordre), puis les comptes de test. Révoquer la session staff créée avec `POST /auth/v1/logout?scope=local` : sans `scope=local`, tous les appareils du compte sont déconnectés.

**Tester les droits (RLS, contraintes, triggers) sans rien écrire en production** : un bloc `DO` exécuté par l'API de gestion (`POST /v1/projects/<réf>/database/query`) qui applique la migration, crée des comptes et des lignes temporaires (y compris dans `auth.users`), simule chaque rôle avec `set local role authenticated` et `set_config('request.jwt.claims', …)`, note chaque tentative dans des sous-blocs `begin … exception when others`, puis se termine par un `raise exception` portant le résultat : tout est annulé d'office. Un `UPDATE`/`DELETE` refusé par une policy ne lève pas d'erreur, il touche 0 ligne (vérifier `row_count`).

**Vérifier l'affichage du portail parent sur téléphone, sans compte ni base** : une page temporaire à la racine du dépôt (jamais commitée, supprimée ensuite) qui recopie le `<style>` et le style de `#root` de `/parent.html`, écrit une fausse session sous `sb-<réf>-auth-token`, remplace `window.fetch` pour toute URL en `/rest/v1/` ou `/auth/v1/` par des données de test volontairement difficiles (noms très longs, lien sans espace, étape de blessure inconnue, offre pleine, date absente…), puis fait `import("/src/parent/main.jsx")`. C'est la vraie app, sans appel réseau réel ni accord `service_role` à demander ; retirer ensuite la fausse session du `localStorage` du navigateur de test. Dans le volet navigateur : mesurer le débordement avec `documentElement.scrollWidth > clientWidth` (pas `innerWidth`, qui grandit jusqu'à la largeur du contenu qui déborde et masque le défaut) ; faire les captures au préréglage mobile 375 px (une taille personnalisée comme 320×568 renvoie une image vide après un défilement) et mesurer 320/360/390 par le code.

**Contrôler une zone DNS sans attendre que Gregory revienne dire « c'est fait »** : `dig` directement contre les serveurs de noms du fournisseur (ex. `dig +short @dns111.ovh.net TXT sous-domaine.domaine`) donne l'état réel, publié ou non, sans attendre la propagation publique ; comparer ensuite avec `@8.8.8.8`/`@1.1.1.1` pour confirmer que c'est bien visible depuis l'extérieur. Une boucle de sondage de quelques minutes en tâche de fond évite de faire attendre Gregory à chaque ajout d'enregistrement pendant qu'il configure Brevo/Resend de son côté.

**Lire un résultat mail-tester.com sans déplier chaque section à la main** : son adresse de résultat (`mail-tester.com/test-XXXXX`) a un miroir JSON public à `?format=json` — le détail complet (règles SpamAssassin déclenchées, sous-tests du corps du message, authentification, liens) en une requête, sans dépendre de captures d'écran successives.

## Structure des fichiers

```
src/
├── App.jsx                      ← fichier principal, environ 35 000 lignes
├── mannequin/index.jsx          ← pantin articulé (édition de pose, séquences, export vidéo)
├── mannequin/personnage3d.jsx   ← personnage 3D (three, chargé à la demande par React.lazy) : visionneuse, animation, accessoires, cadrage
├── mannequin/personnage3dCore.js ← chargement du personnage (modèle, habillage, cheveux), accessoires, préparation d'un mouvement, cadrage
├── mannequin/mouvements3d.js    ← table des 48 mouvements en 3D : vue, poses réécrites pour la 3D, hauteurs, suspension, accessoires
├── mannequin/retarget3d.js      ← recalage des poses 2D du pantin sur le squelette 3D (visée os par os, genoux, pieds, sol)
├── mannequin/habillage3d.js     ← maillot/short/chaussettes/chaussures ajoutés au corps 3D (coques découpées sur le maillage)
├── assets/models/               ← modèles 3D (Quaternius, CC0) + README de provenance et de conversion
├── ressources/bibliotheque.jsx  ← bibliothèque de ressources pédagogiques
├── styles/css.js                ← feuille de style unique
├── lib/storage.js               ← patch de scoping localStorage — fichier central, rarement modifié
├── lib/utils.js                 ← utilitaires partagés (formatDateFr, playerFullName, todayIso, newId...)
├── lib/portalSync.js            ← backend Supabase côté staff : publier / récupérer, codes d'invitation, liaisons parents
├── lib/supabaseClient.js        ← client Supabase côté staff
├── main.jsx                     ← point d'entrée staff (précharge les caches de matchs IndexedDB avant le premier rendu)
├── videoCompiler.js             ← compilation vidéo (ffmpeg.wasm), importée dynamiquement à l'usage
├── data/starterContent.js       ← banques de départ (exercices, séances-types) : importé dynamiquement au clic sur « Pré-remplir », chunk séparé
├── data/formations.js
├── pad/index.js
└── parent/                      ← app parent séparée (point d'entrée `parent.html`, bundle indépendant d'App.jsx)
    ├── main.jsx, ParentApp.jsx, AuthContext.jsx, supabaseClient.js
    ├── lib/api.js               ← tous les appels Supabase de l'app parent
    └── screens/                 ← LoginScreen, LinkChildScreen, PortailScreen, ForumScreen

parent.html                      ← second point d'entrée Vite (voir `vite.config.js`)
supabase/                        ← `config.toml` + `migrations/` (schéma, RLS, fonction `redeem_invitation_code`)
video-pipeline/                  ← pipeline Python d'extraction vidéo
```

- La bibliothèque d'exercices contient 810 exercices (`src/data/starterContent.js`, 67 tableaux `STARTER_EXERCISES*`, dont `STARTER_EXERCISES_SOURCES_2026`, 38 exercices tirés des sources fédérales, `STARTER_EXERCISES_FFF_GIFE_2026`, 42 exercices du Guide interactif du football des enfants de la FFF, `STARTER_EXERCISES_SFV_2026`, 24 exercices des fiches « Kinderfussball » de l'ASF suisse, `STARTER_EXERCISES_FACR_U89_2026` (34 exercices), `STARTER_EXERCISES_FACR_U1011_2026` (25), `STARTER_EXERCISES_FACR_U1213_2026` (20) et `STARTER_EXERCISES_FACR_DIVERS_2026` (11), tirés des manuels tchèques U8-U9, U10-U11, U12-U13, du manuel de rencontre U6-U7 et d'articles de la revue « Fotbal a trénink », `STARTER_EXERCISES_FPF_FUTSAL_2026` (20 exercices futsal du document de formation de la fédération portugaise) et `STARTER_EXERCISES_FA_2026` (32 exercices des séances d'England Football Learning ; voir « Depuis cette transmission »), et `STARTER_EXERCISES_FUTSAL_2026`, 24 exercices, première banque Futsal — jusque-là un onglet vide malgré un `ageFormat: "futsal"` et un rendu de terrain dédié, `FutsalCourtBackground`, déjà prêts côté `App.jsx`), chacun avec les 5 champs de description (déroulé, comportement attendu, critères de réalisation, points clés, variantes). S'y ajoutent 37 exercices spécifiques gardien (`STARTER_GOALKEEPER_EXERCISES`, dans `App.jsx` et non `starterContent.js` — un fichier séparé à vérifier en plus de `starterContent.js` pour tout chantier futur sur le contenu des exercices), au même format à 5 champs, importables depuis Vestiaire → Séance → Gardien.
- `AdvancedAnalyticsPanel` a un callback `onImport` et un schéma `emptyAdvancedAnalytics()` déjà prêts à recevoir les futures sorties du pipeline vidéo.
- Le build Vite (`npm run build`) produit un bundle staff d'environ 2,2 Mo (≈530 Ko gzip) avec un avertissement de taille — pas bloquant, mais réel : temps de chargement à surveiller, en particulier pour un usage mobile en bord de terrain avec un signal faible. La page parent est un bundle séparé (≈14 Ko) mais charge aussi le chunk partagé du client Supabase (≈370 Ko, ≈105 Ko gzip) : ≈110 Ko gzip au total, très en dessous du staff, mais pas « quelques Ko ». Passer `@ffmpeg` en import dynamique n'a gagné que ~7 Ko (le WASM est de toute façon chargé depuis un CDN) : le vrai poids vient probablement de `recharts`/`lucide-react` et d'`App.jsx` lui-même (non mesuré). Depuis le 26/09/2026 `starterContent.js` (≈750 Ko de source, ≈166 Ko gzip) n'est plus dans le bundle principal : `loadStarterContent()` l'importe au clic sur « Pré-remplir » / « Mettre à jour » (quatre gestionnaires dans `App.jsx`), ce qui a ramené le bundle principal à ≈2,08 Mo (≈500 Ko gzip) et laisse ajouter des exercices sans alourdir le premier chargement.

## Processus de travail établi

Chaque ajout ou modification a suivi ce processus, sans exception, sur 13+ sessions :

1. **Vérifier que la fonctionnalité n'existe pas déjà**, avant de la construire — grep sous plusieurs noms plausibles, pas seulement le terme utilisé dans la demande d'origine. Le compte-rendu détaillé plus bas liste plusieurs cas où une fonctionnalité "proposée" existait déjà, parfois construite sous un nom très différent.
2. Extraire les fonctions en dehors des composants avant de les modifier, quand nécessaire pour la conformité aux règles des hooks React.
3. Sur chaque changement, dans cet ordre :
   - Vérificateur de syntaxe
   - Vérificateur de variables non définies (`check_undefined_vars.mjs`) — sur `App.jsx` ET tout fichier séparé touché dans le même chantier
   - Vérificateur de doublons de déclaration de fonction
   - Vraie compilation esbuild (jamais simulée) :
     ```
     esbuild App.jsx --bundle --loader:.js=jsx --loader:.jsx=jsx \
       --external:react --external:react-dom --external:recharts --external:lucide-react \
       --external:./videoCompiler --external:./videoCompiler.js \
       --outfile=/tmp/bundle_check.js
     ```
4. Écrire un fichier de test dédié par chantier, avec des cas qui vérifient la logique réelle (pas seulement la présence de texte dans le code).
5. Faire tourner l'intégralité de la suite de régression (70+ fichiers de test accumulés) avant de considérer un chantier terminé.
6. Ne livrer qu'une fois tout vert.

**Deux points d'honnêteté sur ce processus, à ajuster selon l'environnement réel :**
- Ces vérifications ont été faites dans un environnement de travail sans accès au vrai `npm`/Vite du projet — la commande esbuild ci-dessus est une approximation manuelle de la compilation réelle. Claude Code, s'il tourne dans le vrai dépôt avec `npm install` déjà fait, devrait pouvoir lancer directement `npm run build` — la commande exacte que Vercel exécute en production — ce qui est une vérification plus fiable que cette approximation.
- Les 70+ fichiers de test accumulés vivent dans l'environnement de travail où ce projet a été développé, pas forcément dans le dépôt GitHub réel — ils n'ont jamais été explicitement livrés à Gregory pour qu'il les commit. Si Claude Code ne les trouve pas dans le dépôt, ce n'est pas une régression : il faudra soit demander à Gregory s'il les a conservés, soit reconstruire une suite de vérification équivalente au fil des prochains chantiers.

**Constat en sept. 2026, dans le vrai dépôt** : ni les vérificateurs (`check_undefined_vars.mjs`…) ni les 70+ fichiers de test n'y existent — confirmé, ce n'est pas une régression. Ce qui a été fait à la place pour chaque chantier : `npm run build` (celui de Vercel), `npm run dev` + navigation réelle dans le navigateur sur les écrans touchés, et pour Supabase des requêtes REST avec et sans session sur des comptes de test créés puis supprimés. Aucun test automatisé n'a été ajouté : c'est de la dette, pas un choix.

Quand un chantier touche à la fois `App.jsx` et un fichier séparé (`bibliotheque.jsx`, `mannequin/index.jsx`, `lib/storage.js`...), les deux doivent être livrés et déployés ensemble — un décalage entre les deux a déjà cassé un build en production (import cross-fichier cassé, un fichier mis à jour sans l'autre).

## Leçons techniques — à lire avant de construire ou déboguer

- Toujours vérifier qu'une fonctionnalité n'existe pas déjà avant de la construire. Cas rencontrés dans une seule session : un système de formations continues du staff (existait sous `ClubTrainingScreen`, nom différent de celui cherché), une prévision de couverture arbitrale (existait sous `CouvertureArbitraleTab`), un inventaire matériel (existait sous `InventaireMaterielTab`).
- `computeDirigeantAlerts` doit rester une fonction top-level pour la conformité aux règles des hooks — le suivi de résolution tourne dans un `useEffect` avant un retour anticipé.
- `LIBRARY_PEDAGOGY_THEMES` est intentionnellement dupliqué dans `bibliotheque.jsx` — aucun chemin d'import direct de ce module vers les constantes d'`App.jsx`, documenté par un commentaire. Mais toutes les duplications apparentes ne sont pas nécessaires : `daysSinceStatusChange` s'est avéré déjà exporté et importé, juste jamais appelé — toujours vérifier les déclarations `export`/`import` réelles avant de supposer qu'une duplication est nécessaire.
- Les assertions de test qui cherchent une chaîne présente dans plusieurs composants doivent être bornées aux limites de la fonction ciblée (`src.slice(src.indexOf("function X"), src.indexOf("\nfunction ", start + 10))`) — les clés de stockage et autres littéraux apparaissent souvent à la fois côté lecture et côté écriture, dans des composants différents.
- Les tests de présence sur des fenêtres de caractères fixes se cassent quand les fonctions grossissent — préférer découper sur les limites réelles de fonction.
- La détection de doublons doit matcher sur nom ET date de naissance pour éviter les faux positifs.
- Un joueur du roster chargé depuis la clé scopée `tf_roster` n'a pas de champ natif `teamId`/`teamIds` — il fait implicitement partie du roster scopé de son équipe. `teamId` n'apparaît que sur des objets synthétiques construits pour des vues transversales (ex. `findLinkedMultiTeamPlayers`), jamais sur le joueur brut stocké.
- `tf_exercices` (référencé par les séances-types) est scopé par équipe, PAS la même chose que `tf_club_exercise_library` (la vraie bibliothèque partagée, non-scopée, club-wide) — une séance-type construite par une équipe peut référencer des identifiants d'exercices invalides dans la banque d'une autre équipe ; résoudre par identifiant d'abord, puis par nom en repli.
- Chaque banque `STARTER_X` a son bouton « Importer... (N) », mais N est un chiffre écrit en dur dans le JSX, jamais calculé depuis `STARTER_X.length` — étendre une banque sans grep systématiquement `Importer.*[0-9])` (ou équivalent `Pré-remplir`/`étapes`/`modèles`) après coup laisse un compte affiché faux, silencieusement, jusqu'à ce que quelqu'un compte à la main. Un cas réel m'a échappé une première fois (packages de sponsoring, resté affiché à 4 après passage à 6) avant d'être rattrapé par un script de vérification systématique.
- Le contenu des exercices n'est pas entièrement dans `src/data/starterContent.js` : `STARTER_GOALKEEPER_EXERCISES` (37 exercices spécifiques gardien) vit dans `App.jsx`, importé séparément depuis Vestiaire → Séance → Gardien. Trouvé en sept. 2026 en cherchant pourquoi l'onglet Gardien restait vide après le passage au format à 5 champs sur `starterContent.js` — son écran de fiche (`BibliothequeGardienScreen`) avait aussi sa propre vue minimaliste au lieu de réutiliser `ExerciseDetailModal`, donc les nouveaux champs ne s'affichaient pas même une fois ajoutés aux données. Toujours vérifier les deux fichiers pour un chantier sur le contenu des exercices, et vérifier que chaque écran de fiche réutilise bien le même composant d'affichage plutôt que d'en dupliquer un.
- Déplacer ou renommer l'emplacement d'un écran demande de chercher dans tout le fichier les références obsolètes au-delà du seul routage : entrées de mots-clés de l'assistant, réponses de l'aide sur le site, messages `alert()` visibles par l'utilisateur, et libellés `eyebrow` à l'intérieur des écrans déplacés eux-mêmes. Quinze références dispersées trouvées lors du déplacement du Pôle sportif vers Vue académie.
- Les lignes d'onglets avec beaucoup de boutons ont besoin de `flexWrap: "wrap"` sur la div `.tabs` — motif déjà utilisé par 20+ écrans, pas une solution ponctuelle à chaque fois.
- Un écran dont le conteneur externe n'a pas la classe `stats-screen` perd le centrage et la largeur maximale (`margin: 0 auto; max-width: 1000px` dans cette classe, c'est elle qui centre tous les autres écrans) — vérifier cette classe en premier quand une mise en page semble poussée d'un côté.
- Un composant avec son propre conteneur `dashboard-card` complet (ex. `PlayerOfTheMonthWidget`) ne peut pas être réutilisé tel quel dans le système de widgets de l'Accueil, qui enveloppe déjà chaque widget dans son propre encart — extraire le calcul interne en fonction partagée plutôt que d'imbriquer deux encarts.
- Un élément natif non contrôlé (ex. `<details>`) qui reçoit un prop `open` contrôlé par React DOIT aussi recevoir un gestionnaire `onToggle` qui synchronise l'état, sinon l'élément devient insensible aux clics manuels de l'utilisateur.
- Une suspension (accumulation de cartons jaunes) n'a pas de durée fixe en jours — elle se purge en manquant le prochain match. `detectYellowCardSuspensions` ne calcule que la date de déclenchement (`suspensionStart`), jamais la fin. Approximation retenue : une suspension est considérée purgée dès qu'un match joué (pas d'observation) a eu lieu depuis `suspensionStart` — l'app ne peut pas vérifier la conformité réelle du jour J, seulement le calendrier.
- Un identifiant de joueur (`player.id`) survit à une reconduction de saison — `tf_roster` est recopié tel quel (JSON brut, identifiants inclus) d'une saison à l'autre. Une liste de suivi indexée par `playerId` reste donc valide dans le temps, à condition d'être stockée dans une clé non-scopée pour rester continue plutôt que remise à zéro chaque saison.
- **Identifiants issus de `localStorage` : aucun format garanti.** Le vrai roster contient des `id` hérités comme `"eff-01"`, pas des UUID. Toute colonne Postgres alimentée par `localStorage` doit être en `text` (seuls les identifiants auto-générés par Postgres et les références à `auth.users` sont en `uuid`). Un `ALTER COLUMN TYPE` échoue dès que des clés étrangères ou des policies RLS dépendent de la colonne : la migration `20260912010000_fix_id_types_full_rebuild.sql` a reconstruit les tables, ce qui n'est possible que tant qu'elles ne contiennent pas de vraies données.
- `tf_club_faq` et `tf_club_carpool` ne sont **pas** dans `UNSCOPED_STORAGE_KEYS` malgré leur préfixe `tf_club_` : elles sont scopées par équipe/saison, contrairement à `tf_club_events`, `tf_club_staff`… — vérifier la liste réelle, jamais le préfixe (à confirmer avec Gregory si c'est voulu).
- `readMatchFromCache` est synchrone et ne lit qu'un cache mémoire rempli pour l'équipe/saison **active** : `main.jsx` le précharge avant le premier rendu (`loadMatchCacheOnce`, `loadObsMatchCacheOnce`) et `loadMatchesForScope(teamId, seasonId)` charge à la demande une autre saison (sans ça, la comparaison avec une saison passée affichait zéro partout). Tout écran qui lit ce cache pour une autre équipe/saison doit passer par là.
- Un builder `supabase-js` (`supabase.from(...).upsert(...)`) n'envoie sa requête qu'à l'`await` ou au `.then()` : sans l'un des deux, rien ne part et aucune erreur ne remonte.
- Une jointure PostgREST (`select("*, autre_table(...)")`) exige une clé étrangère directe entre les deux tables : `parent_player_links` et `parent_profiles` référencent chacune `auth.users` mais pas l'une l'autre, la jointure échouait (`PGRST200`) et l'écran staff « Parents liés » avalait l'erreur en affichant « aucun parent lié ». `listPlayerLinks` fait donc deux requêtes, et le staff lit `parent_profiles` grâce à une policy dédiée. Un écran qui attrape toutes les erreurs et affiche un état vide masque ce genre de défaut : tester avec des données présentes, pas seulement vides.
- RLS : sans policy `INSERT`/`UPDATE`/`DELETE`, personne n'écrit, pas même le staff ; sans policy `SELECT` pour `is_staff()` sur les tables écrites par les parents, la récupération côté staff renvoie silencieusement 0 ligne. Tester chaque rôle réellement, avec et sans session, sur des données existantes.
- Deux alertes `npm audit` (`vite`/`esbuild`) ne concernent que le serveur de dev, pas le build de production ; leur correctif passe par Vite 8 (changement majeur), non fait.
- glTF / three : les attributs d'un maillage chargé par `GLTFLoader` peuvent être **entrelacés et normalisés** — ne jamais lire `attribute.array[i * 4 + k]` (on obtient des indices d'os inexistants, et le rendu plante sur `matrixWorld` d'un os `undefined`, à chaque image) ; passer par `getX/getY/getZ/getW` et écrire les copies dans des tableaux simples. Une boucle `setAnimationLoop` doit attraper ses erreurs (arrêt + repli), sinon elle se répète à chaque image. Côté conversion : un `.gltf` du pack Quaternius référence des textures absentes (fichiers renommés), et `@gltf-transform` ne supprime pas seul les échantillonneurs d'une animation retirée (les disposer un à un, sinon le fichier garde tout son poids : 5 Mo au lieu de 200 Ko).

## État du projet à la date de cette transmission

Trois vagues de propositions de fonctionnalités ont été systématiquement travaillées et vérifiées contre le code réel : un document de 32 propositions, un second de 36, et un document plus ancien et plus large de 150 propositions couvrant 50 rôles du club. L'écrasante majorité est construite et vérifiée — un échantillonnage large et varié du troisième document (y compris des rôles très spécifiques comme Podologue) n'a trouvé aucun manque.

Constructions récentes notables : système de widgets réorganisables sur l'Accueil (19 widgets au total), tarifs de cotisations par catégorie/gardien/féminine, modèles de créneaux bénévoles avec génération automatique à la création d'un événement, covoiturage visible côté portail parent, déplacement structurel du Pôle sportif vers Vue académie, reconnaissance des bénévoles, liste de suivi de talents internes distincte du scouting externe, total des dépenses matériel toutes acquisitions confondues, et disponibilité de l'effectif couvrant désormais blessures et suspensions.

Chercher de nouvelles fonctionnalités à ce stade a des rendements décroissants. La prochaine étape à plus forte valeur n'est plus de construire, mais de confronter l'app à un usage réel — un document de questions d'entretien par rôle a été rédigé à cet effet (même remarque que pour les tests : produit dans l'environnement de travail, à demander à Gregory s'il l'a conservé).

### Depuis cette transmission (sept. 2026)

- Correctifs issus d'une revue du dépôt : la comparaison avec une saison passée affichait zéro (cache de matchs jamais chargé pour l'autre saison) ; préchargement des caches de matchs avant le premier rendu (course avec IndexedDB) ; garde `sessionStorage` et alerte de quota dans `lib/storage.js`.
- Backend Supabase du portail parent et du forum, app parent séparée : voir la section dédiée. Testé en production avec l'effectif réel (20 joueurs, 20 matchs, 3 compétitions, 29 rencontres publiés). Un seul lien parent↔joueur existe à ce jour (issu des tests) : aucune famille réelle n'a encore été invitée.
- Pipeline vidéo intégré au dépôt (`video-pipeline/`, une quarantaine de commits du 12 au 18/09).
- App parent vérifiée sur téléphone (375, 360, 390 et 320 px) puis corrigée : champs à 16 px, cibles tactiles à 44 px, étape de blessure lisible au lieu de la clé technique, dates en français, bouton « Complet » grisé et boutons de covoiturage espacés du texte, en-tête aligné, liens longs qui ne débordent plus. Un lien sans point de coupure débordait de sa carte (mesuré : 596 px de contenu sur 375) alors que la première vérification l'avait jugé « OK » : toujours tester avec un mot sans espace, pas seulement de longues phrases.
- Domaine `game-changer.fr` acheté et authentifié, SMTP passé de Supabase par défaut à Brevo (secours) puis à Resend (actif, 10/10 sur mail-tester) : voir « Email et connexion » pour le détail, la limite encore ouverte (iCloud classe toujours en indésirables, question de réputation du domaine plus que de configuration) et le piège du copier-coller d'un modèle d'email (accents corrompus).
- Fiche exercice enrichie à 5 champs (déroulé/comportement attendu/critères de réalisation/points clés/variantes), tour de fédérations élargi de 5 à 20, premier lot Foot à 4 (15 exercices, tranche jusque-là inexistante), 56 nouveaux exercices au total — puis le même format à 5 champs rétrofité sur les 475 exercices pré-existants (registre adapté par tranche : ludique pour Foot à 4/5, transitoire pour Foot à 8, technique pour standard et les tableaux génériques). Bibliothèque complète (531 exercices) désormais homogène. Au passage : 25 descriptions vides pré-existantes comblées (RBFA, DFB_1, DFB_2, plusieurs tableaux NATIONS) et 4 exercices DFB_1 « Bambini » avec un `ageFormat` obsolète corrigés (`foot_a_5` → `foot_a_4`, même classe de bug que déjà rencontrée sur FFF/RBFA). Puis même format appliqué aux 30 exercices gardien (`STARTER_GOALKEEPER_EXERCISES`, dans `App.jsx`) et `BibliothequeGardienScreen` corrigé pour réutiliser `ExerciseDetailModal` au lieu de sa propre vue minimaliste qui n'affichait ni les nouveaux champs ni le diagramme. Puis première banque Futsal (24 exercices, `STARTER_EXERCISES_FUTSAL_2026`) : contenu pensé comme une vraie discipline (règle des 4 secondes du gardien, powerplay, fautes cumulées, jeu de pivot, espace réduit), pas du foot à 5 générique — l'onglet Futsal de la Banque générale existait déjà dans l'interface mais restait vide faute de contenu.
- Au-delà de la bibliothèque d'exercices, le reste de l'app suit le même motif « `STARTER_X` + bouton d'import dédupliqué par titre » pour une bonne quinzaine de banques de contenu de départ (prévention par poste, techniques de gestion du stress, programmes RTP, exercices de rééducation, nutrition, chaussage, échauffement gardien, formations tactiques, référentiel pédagogique, modèles de séance, principes de méthodologie, courriers types...) — jamais audité avant sept. 2026. Première passe : `STARTER_FEMININE_RESOURCES` créé (17 ressources sur les spécificités physiologiques du football féminin — LCA, cycle menstruel, pédagogie — plus le bouton d'import qui manquait entièrement sur `ResourcesFeminineTab`, seul vrai vide au même titre que le Futsal avant cette session) ; `STARTER_GOALKEEPER_WARMUP` enrichi d'un champ `notes` (titre + durée seuls auparavant, aucune consigne) côté donnée ET côté écran (`EchauffementGardienScreen`) ; `STARTER_PREVENTION_PROTOCOLS` et `STARTER_STRESS_TECHNIQUES` complétés (20→28, 17→21) plutôt que réécrits, leur contenu existant étant déjà correct. Deuxième passe (même soirée) : nutrition, chaussage, exercices de rééducation, référentiel pédagogique et modèles de séance relus en détail — déjà de bonne qualité, laissés tels quels plutôt que réécrits sans raison (les modèles de séance référencent des exercices par nom exact, résolu à l'id réel à l'import : les 31 noms utilisés ont été vérifiés un par un contre `starterContent.js`, aucun cassé). Deux vrais manques trouvés et comblés : programmes RTP sans aucune couverture « Osseuse/Fracture » et quasi aucune en « Tendineuse » malgré ces deux catégories déjà couvertes côté `STARTER_REHAB_EXERCISES` — ajout de « Fracture de fatigue du métatarsien » et « Tendinopathie achilléenne » (6→8) ; principes de méthodologie clairsemés (6) pour ce qu'ils couvrent réellement — 4 ajoutés (6→10).

  Gregory a ensuite autorisé la suite en autonomie complète (« travaille au maximum sans demander d'autorisation »), pour cause d'indisponibilité nocturne — troisième passe, plus large, sur le reste des banques repérées à l'audit initial : modèles de courriers (10→18, dont un modèle d'avertissement disciplinaire au ton volontairement factuel et orienté dialogue plutôt que punitif), séances-types (10→16 modèles, 6 nouveaux thèmes dont les exercices référencés ont été vérifiés comme les précédents), exercices de rééducation (25→34, ajout de la catégorie Commotion entièrement absente alors que les programmes RTP et le protocole commotion la couvrent déjà), nutrition (15→24), chaussage (12→16), référentiel pédagogique (une priorité de plus par domaine sur les 17 catégories, 85→153 priorités), et packages de sponsoring (4→6). Toutes ces additions vérifiées comme les précédentes (validateur/build/navigateur réel), commitées et pushées au fil de l'eau sans confirmation individuelle — seule bibliothèque encore non retouchée : `STARTER_FOOTWEAR_RECOMMENDATIONS` avant la 2ᵉ passe l'était déjà, donc l'ensemble des ~19 banques de départ identifiées à l'audit a été revu au moins une fois. Une erreur trouvée et corrigée après coup : plusieurs boutons « Importer... (N) » avaient un compte figé dans le JSX, pas recalculé depuis le tableau — un decompte systématique de tous les boutons contre la taille réelle de leur tableau (script Node) a détecté et corrigé un cas oublié (packages de sponsoring, resté à « (4) » après le passage à 6) ; à refaire après toute future extension de l'une de ces banques plutôt que de supposer le chiffre à jour.
- Pantin articulé (`src/mannequin/index.jsx`, Préparation physique → Physique → Mouvements animés), sur demande de Gregory : le style « réaliste » (membres effilés, ajouté lors d'une session précédente) n'avait encore aucun réglage — `MannequinPreview` ignorait silencieusement toute proportion qui lui était passée pour ce style — corrigé avec un panneau complet, même architecture que le style « actuel » (`REALISTIC_SLIDER_GROUPS`, proportions sauvegardées à part sous `tf_mannequin_proportions_realiste`). Puis un troisième style, « Silhouette humaine (proposition) » : bras et jambes en une seule silhouette continue (épaule→coude→poignet, hanche→genou→cheville, une courbe de Bézier quadratique par côté via `drawLimbSilhouette`) au lieu de deux segments effilés raccordés par un rond au coude/genou — élimine l'effet « pantin en bois à boules » des deux styles précédents, confirmé en comparant les trois styles côte à côte sur un vrai mouvement (Squat), pas seulement sur la position de référence. Torse aux mêmes quatre largeurs (épaule/poitrine/taille/hanche) que le style réaliste mais reliées par des courbes plutôt que des segments droits (`smoothSidePath`, `drawTorsoHumain`). Son propre panneau de réglages dès le premier commit (`HUMAIN_SLIDER_GROUPS`, `tf_mannequin_proportions_humain`), testé en direct (slider → mise à jour du canvas en temps réel).
- Gregory, après avoir vu le style « Silhouette humaine » : pour lui, restait surtout une légère variation du style réaliste (les deux dessinent un contour — polygone ou courbe). Demande de comparer les styles en vrai et, si possible, d'en proposer un sur un principe différent plutôt qu'un raffinement du même. Un 4ᵉ style ajouté, « Style tubulaire (proposition) » : aucun contour calculé — chaque bras, jambe et le torse sont une chaîne de cercles pleins superposés (`drawTubeChain`, réutilisée pour les membres à 3 points et le torse à 4 points) ; un cercle n'ayant pas de coin, la rondeur à un coude/genou est automatique quel que soit l'angle, sans courbe à raccorder ni rond à poser par-dessus. Vérifié robuste sur une pose à membres croisés (Grimpeur, genou remonté devant le torse), pas seulement des poses simples. Piège rencontré et corrigé : un nombre de cercles fixe par segment donnait un bord en « pile de pièces » visible dès qu'on agrandit le canvas (aperçu large, zoom navigateur) — corrigé en espaçant les cercles sur la distance réelle en pixels plutôt qu'un compte fixe. En construisant ce 4ᵉ style, une vraie incohérence trouvée dans le 3ᵉ (« Silhouette humaine ») : `drawLimbSilhouette` utilisait ses largeurs de bras/jambe comme un rayon au lieu d'une épaisseur (contrairement à `drawTaperedLimb` et au reste du fichier), rendant les membres deux fois plus épais que voulu à réglage égal avec les autres styles — signalé à Gregory avant de corriger (le style avait déjà été montré/commenté), corrigé sur sa confirmation.
- Personnage 3D (« Personnage 3D (bêta) », même écran), sur demande de Gregory (« un vrai personnage 3D, garçon et fille », après avoir écarté les styles 2D, jugés trop proches les uns des autres ; un premier essai 2D « Personnage » habillé a été abandonné, jamais commité). `three` est la seule nouvelle dépendance, chargée à la demande par `React.lazy` : chunk `personnage3d` ≈ 174 Ko gzip, bundle principal inchangé (≈ 641,5 Ko gzip), modèles ≈ 1 Mo chargés seulement à l'ouverture du style. **Modèles** : Quaternius « Universal Base Characters » version Standard (CC0, gratuite) — **uniquement les deux « Superhero »** (adultes stylisés ; les Regular/Teen sont dans la version payante), livrés avec des sous-vêtements peints dans la texture du corps. Ils sont retirés de la texture à la conversion (outillage jetable `gltf-transform` + `sharp`, non versionné, décrit dans `src/assets/models/README.md`) et recouverts au chargement par un maillot, un short, des chaussettes et des chaussures (`habillage3d.js` : découpe exacte des triangles du corps par des plans calés sur les os, décalés le long des normales, mêmes os et poids de peau) ; **le modèle n'est ajouté à la scène qu'une fois habillé**, jamais en tenue de base. Cheveux : coiffures du pack rattachées à l'os `Head`, teintées (leur texture est en niveaux de gris). **Poses (les 48 mouvements)** : `retarget3d.js` tourne chaque os pour qu'il prenne la direction 2D du segment de la pose (longueurs d'os du modèle respectées, arc le plus court pour ce que le 2D ne dit pas), avec fondu entre poses-clés. `mouvements3d.js` (`MOUVEMENTS_3D`) dit, par mouvement, ce que le dessin ne dit pas : `facing` (`front` de face, `right`/`left` de profil ; pour un mouvement allongé, ce sens décide ventre ou dos — tourné du côté de la tête, sur le ventre), et facultativement `poses` (poses réécrites pour la 3D à partir d'angles par segment, `poseFromAngles` : traction, dips, squat bulgare, fente arrière, tacle glissé — leur dessin d'origine n'était pas exploitable), `heights` (hauteur du point le plus bas par pose : sauts, plongeon), `hang` (suspension : mains à la barre, pieds à N m du sol dans la première pose) et `props` (barre, barres parallèles, banc : primitives three dans `personnage3dCore.js`, retirées et libérées à chaque changement de mouvement). Seul le sprint passe par une animation toute faite (`CLIPS_3D`, `Sprint_Loop`). Un mouvement absent de la table montrerait le personnage au repos (`Idle_Loop`) avec un bandeau explicite — aujourd'hui les 48 y sont. Règles de recalage apprises : en vue de face, le côté gauche/droit du personnage se décide à l'ordre x dessiné ; un genou dessiné derrière la hanche (squat) est refait par une résolution à deux os (pôle = avant du bassin) ; le pied reste à plat sauf frappe ou dessin qui s'écarte du tibia ; le point le plus bas est plaqué au sol (`guessAirHeight` pour les sauts) ; `computeFraming` cadre chaque mouvement (toutes ses poses et ses accessoires) en gardant l'angle de vue choisi par l'utilisateur. **Vérifié** : build (bundle principal inchangé), planches-contact des 48 mouvements rendues et relues dans le navigateur, test Node jetable sur squelette synthétique (non versionné : direction de chaque os à moins de 1e-3, genou, pied, fondu, idempotence, côtés), dans l'app réelle traction/dips/squat bulgare avec leurs accessoires (rien ne reste au changement de mouvement), Fille sur un mouvement à accessoire, les 48 mouvements enchaînés sans bandeau « pas encore animé », 30 bascules de style sans fuite de contexte WebGL ni avertissement, mobile 375 px sans débordement, export vidéo du canvas WebGL (`captureStream`). **Limites connues** : certaines poses restent approximatives (dessins 2D d'origine ambigus en profondeur et en torsion, contacts des pieds au sol approximatifs, tacle glissé lu comme une glissade sur le dos) ; corps adultes seulement ; pas encore dans l'Enchaînement (`SequenceBuilder`/`SequenceRunner`) ni l'éditeur de pose (repli sur le style tubulaire avec une note) ; couleurs du club figées dans le code ; Safari/iOS réel non testé. **Pièges** : `THREE.Clock` est déprécié depuis three 0.186 (l'intervalle entre images est calculé à la main) ; en test dans le navigateur, une boucle qui clique tous les boutons du sélecteur clique aussi « Exporter en vidéo » et télécharge un fichier.
- Exercices tirés des liens de fédérations (sept. 2026), sur demande de Gregory qui a fourni deux documents de liens (`05_Liens_sources_groupe_C.docx`, `06_Liens_sources_groupes_A_B.docx`, dans ses Téléchargements, jamais versionnés) : `STARTER_EXERCISES_SOURCES_2026` (38 exercices : 13 défis et format 2c2 U6 belges ACFF/RBFA, 13 jeux de The FA dont 12 « Play Phase » 4-6 ans, 2 fiches FIGC, 3 exercices de la séance inclusive du DFB, 4 exercices des Sub-20 de l'AFA, 3 jeux de possession avec gardiens jokers de la SvFF) et 7 exercices gardien SvFF ajoutés à `STARTER_GOALKEEPER_EXERCISES`. **Tri sans doublons** : l'import du site ne compare que le nom exact, donc une même idée sous deux noms passe ; chaque candidat a été comparé au contenu (TF-IDF sur nom + description, puis relecture — le lexical seul rate « 1-2-3 soleil » face à « 1, 2, 3 ballon »), classé nouveau / déjà présent / variante, et les jeux voisins fusionnés en un seul exercice avec leurs variantes (famille canonique : chasseur + chat et souris + jumeaux ; déménageurs + écureuils ; bowling + tireur d'élite ; équilibristes + sac de graines). Non repris car déjà présents : « 1-2-3 soleil » (= « 1, 2, 3 ballon »), « Le duel » (= « Duel vers les deux petits buts »), les 6 autres fiches FIGC du manuel CFT (cage aux lions, statues, zoo, passage à gué, chacun pour soi, match modulaire), requins et poissons (= « Le jeu de l'épervier »). **Nouveau champ `source`** (texte libre : saisie dans le formulaire d'exercice, ligne « Source » dans `ExerciseDetailModal`) pour créditer l'origine ; les textes et schémas sont réécrits, jamais recopiés (dépôt public, droits d'auteur). **Illisibles ou sans exercice** : URBSFA (méthodologie pure), articles méthodo de The FA, KNVB Rinus (titres seuls, détail derrière un compte), banques DBU et SvFF (appli ou compte, sauf 10 exercices publics), vidéos HNS, FCF (préparation physique des arbitres), portails AUF/JFA/CBF/FMF/FSS/FPF, RFEF (séances en images). **Bloqués pour Claude (Cloudflare/403), téléchargés ensuite par Gregory (20 PDF le 26/09/2026)** : FFF GIFE U6-U7, U8-U9, U10-U11 et infos générales, guide futsal FFF, SFV Trainingskarten E/F/G, FPF futsal, 11 documents FAČR. **Toujours manquants** : FFF GIFE U12-U13, FIGC eserciziario, Formafoot. **GIFE de la FFF traité** (`STARTER_EXERCISES_FFF_GIFE_2026` : 12 exercices U6-U7 en Foot à 4, 11 U8-U9 en Foot à 5, 19 U10-U11 en Foot à 8 dont 2 échauffements, le golf-foot et le duo-foot) : le texte des PDF vient de PDFKit, avec l'OCR Vision en repli ; les schémas animés de la version interactive n'ont ni texte ni image exploitable, donc les diagrammes sont reconstruits à partir du texte. Les jeux du GIFE déjà présents (déménageurs, château/quilles, épervier, 1-2-3 soleil, sorciers, magicien, pyramide, quatre coins) n'ont pas été redoublés : leurs variantes ont été ajoutées aux fiches existantes. **Fiches SFV traitées** (`STARTER_EXERCISES_SFV_2026` : 24 exercices, 7 pour la catégorie G, 9 pour F et 8 pour E ; chaque catégorie a trois entraînements de 75 à 90 minutes construits sur « ouvrir, pratiquer, comparer ») : rangées dans les tranches suisses existantes selon l'âge (G ≈ 5-6 ans → `ch_f`, F ≈ 7-8 ans → `ch_e`, E ≈ 9-10 ans → `ch_d`) ; le bloc « 4 contre 4 avec 2 contre 2 sur un terrain annexe » se répète à l'identique dans les 3 entraînements et n'est donc écrit qu'une fois par format (G/F, puis E à 5 contre 5). Six jeux voisins ont été repliés en variantes de fiches existantes (sorcier, bowling, chasse au trésor, déménageurs, golf du but, 2 contre 1). **Manuel FAČR U8-U9 traité** (`STARTER_EXERCISES_FACR_U89_2026` : 34 exercices, séries A « habiletés individuelles », B « force, vitesse, agilité » et C-D « jeux et jeux préparatoires ») ; la Tchéquie est ajoutée comme fédération de référence dans `CURRICULUM_FEDERATIONS` (clé `FACR`) avec quatre tranches `cz_u67`, `cz_u89`, `cz_u1011`, `cz_u1213` dans `CURRICULUM_BRACKETS` (`App.jsx`). Six jeux voisins repliés en variantes (1-2-3, stand de tir, déménageurs, chasseur et lièvre, épervier, ballon apprivoisé). **Manuel FAČR U10-U11 traité** (`STARTER_EXERCISES_FACR_U1011_2026` : 25 exercices ; six jeux repliés en variantes de fiches existantes, dont « bagovka 3:2 », les jeux de force et la ligue des champions ; un exercice de départ couché fusionné avec « course au ballon lancé »). **Manuel FAČR U12-U13 traité** (`STARTER_EXERCISES_FACR_U1213_2026` : 20 exercices, surtout tactiques : sortie de balle, pressing, transitions, jeu de position ; les exercices de force et d'agilité de ce manuel sont identiques à ceux du U10-U11 et n'ont fait qu'ajouter une source aux fiches existantes ; six jeux repliés en variantes). **Compléments FAČR traités** (`STARTER_EXERCISES_FACR_DIVERS_2026`, 11 exercices) : trois formes de rencontre du manuel U6-U7 (tranche `cz_u67`), les stages Benfica et FC Bâle de D. Kadlec, les jeux préparatoires de P. Frýbort ; les trois articles de V. Chaloupka (tir, défense et sortie de balle en U8-U9) reprennent des exercices déjà pris dans les manuels et n'ont rien ajouté ; l'article de Kadlec sur les remplacements est de la méthodologie pure. Les exercices sans tranche tchèque précise (Foot à 11 ou Foot à 8 sans `curriculumFederation`) n'apparaissent que dans la banque générale. **Futsal** : le guide FFF (132 pages) et le document « informations générales » du GIFE (17 pages) ne contiennent aucun exercice (organisation, calendrier, finances, formation) ; le document de la fédération portugaise (`STARTER_EXERCISES_FPF_FUTSAL_2026`) donne quatre niveaux de jeu de dix exercices, écrits sans schéma exploitable (les figures du PDF sont des images) : ils ont été reconstruits d'après le texte et fusionnés en 20 fiches (la banque futsal passe de 24 à 44), sans tranche fédérale propre (pas de tranche futsal dans `CURRICULUM_BRACKETS`). **The FA traité** (`STARTER_EXERCISES_FA_2026` : 32 exercices, tranches `foundation` et `youth_dev`) à partir des pages publiques `learn.englandfootball.com/sessions/resources/…` (197 pages : 127 d'un exercice, 70 sessions de trois pratiques) : les séances varient un même jeu (dribbling, turning, finishing, pressing, interception, marquage), fusionnées en une fiche par jeu ; les pages dont le texte est une vidéo (« Building the attack », « Playing out and through », « Stay on it », « Seven ways to master the ball »…) n'ont pas de contenu lisible et n'ont pas été exploitées, ni les séances sur les gardiens et les programmes de six semaines (redondants avec les séances qu'ils regroupent). Sept jeux voisins sont repliés en variantes de fiches existantes. `starterContent.js` est désormais chargé à la demande (voir la section sur le bundle) : ajouter des exercices ne pèse plus sur le premier chargement.

## Prochaines étapes envisagées

- **Fait** : backend pour une fonctionnalité ciblée, avec le portail parent et le forum (section « Backend Supabase »). L'étendre à d'autres fonctionnalités est une décision à part : le staff reste en `localStorage`.
- **Fait techniquement, pas encore fiable partout** : domaine acheté (`game-changer.fr`), authentifié chez Brevo puis Resend, SMTP branché sur Resend dans Supabase, connexion testée avec une adresse qui n'est pas celle de Gregory. Reste ouvert avant d'inviter plusieurs familles à la fois : iCloud met encore l'email en indésirables (réputation du domaine, pas la configuration — voir « Email et connexion ») ; test Gmail pas encore fait.
- Premier usage réel : quelques familles de confiance pendant une à deux semaines avant de construire davantage — même logique que plus haut, l'usage réel avant les nouvelles fonctionnalités.
- Dette identifiée : bundle staff de ~2,2 Mo ; une quinzaine d'écrans lisent `readMatchFromCache` sans attendre le chargement (atténué par le préchargement dans `main.jsx`, pas éliminé) ; aucun test automatisé ; publication manuelle (oublier « Publier » laisse les parents sur des données périmées) ; marges latérales doublées sur l'app parent (24 px du `#root` de `parent.html` + 16 px de `.page` : 40 px par côté, soit 295 px de contenu sur un écran de 375 — repéré, non corrigé).
- App mobile native une fois le backend en place — React Native pressenti vu que le site est déjà en React, mais attention : tout ce qui est spécifique au navigateur dans le code actuel (`localStorage`, dessin canvas pour le pantin, stockage vidéo, `window.print()`) demande une vraie réécriture pour un environnement natif, pas un portage direct. La base et l'authentification existent désormais ; le modèle de « publication manuelle » devra probablement évoluer pour une app qui doit rester à jour toute seule.
- Le pipeline vidéo (Python/Colab) vit dans `video-pipeline/` ; son état d'avancement se lit dans `git log -- video-pipeline`, il n'est pas détaillé ici.
- Personnage 3D, à trancher avec Gregory : (1) le look — les « Superhero » adultes du pack gratuit, ou les versions Teen (payantes) / une autre source CC0 (le dépôt est public : pas de fichiers bruts Mixamo) ; (2) la suite — repasser sur les poses que Gregory juge fausses en les regardant (les 48 mouvements sont animés, mais certains restent approximatifs : pieds au sol, dessins 2D d'origine ambigus), intégrer aux enchaînements et à l'éditeur de pose, couleurs du club réglables.
